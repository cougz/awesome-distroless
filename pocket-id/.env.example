# Build Configuration (used during Docker build)
APP_UID=1000
APP_GID=1000
TZ=UTC
POCKET_ID_VERSION=1.10.0

# Runtime Configuration
WEB_PORT=3000

# PostgreSQL Database Configuration
POSTGRES_DB=postgres
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# Application Configuration
APP_URL=http://localhost:3000

# System Configuration
TMPFS_SIZE=1G
# Docker Image Configuration (optional)
# Uncomment to use a pre-built image from a registry instead of building locally
# POCKET_ID_IMAGE=ghcr.io/cougz/awesome-distroless/pocket-id:1.10.0
# POSTGRES_IMAGE=ghcr.io/cougz/awesome-distroless/postgres:17.6
# BASE_IMAGE=ghcr.io/cougz/awesome-distroless/base:1.0.0
